# 🧮 Python OOP Calculator v1

This repository contains a simple command-line calculator built using **Python Classes and Objects**.  
The user inputs two numbers, selects an operation, and the calculator performs **Addition**, **Subtraction**, **Division**, or **Multiplication**.

This project was created for a **silent ASMR coding video (no voice)** and focuses on clean structure, readability, and beginner-friendly OOP concepts.

---

## 🔧 Features
- Uses a `Calculator` class  
- Each operation is a separate method  
- Handles division by zero  
- Clean CLI menu  
- Beginner-friendly OOP example  

---

## 📂 Code Overview

### **Calculator Class**
- `add()` — Adds two numbers  
- `subtraction()` — Subtracts second number from the first  
- `divide()` — Divides numbers and prevents division by zero  
- `multi()` — Multiplies two numbers  

---

## 🖥 Main Program
- Accepts user input  
- Creates a `Calculator` object  
- Displays a menu of operations  
- Performs the selected arithmetic  

---

## ▶️ Run the Program

### **1. Install Python 3**

### **2. Run the script**
```bash
python calculator.py
